{
  "name": "fraudsys",
  "version": "1.0.0",
  "scripts": {
    "start": "cd backend && node server.js",
    "build": "cd backend && npm install && pip3 install pandas scikit-learn numpy joblib"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
